import { Typography, Box, styled } from '@mui/material';

const StyledHeaderContainer = styled(Box)({
  margin: '16px 0 50px 0'
});

const StyledTypography = styled(Typography)({
  fontSize: '16px',
  textAlign: 'left'
});

export { StyledHeaderContainer, StyledTypography };
