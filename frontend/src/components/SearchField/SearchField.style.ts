import { styled, TextField } from "@mui/material";

export const StyledTextField = styled(TextField)({
  borderRadius: "4px",
  width: "calc(100% - 60px)",
  margin: "30px",
  marginLeft: "40px",
});
