apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: task-queuer-claim0
  name: task-queuer-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
