apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: alert-scheduler-claim0
  name: alert-scheduler-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
