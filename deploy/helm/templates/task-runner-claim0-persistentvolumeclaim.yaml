apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: task-runner-claim0
  name: task-runner-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
