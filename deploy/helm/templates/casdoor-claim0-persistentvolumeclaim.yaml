apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: casdoor-claim0
  name: casdoor-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
